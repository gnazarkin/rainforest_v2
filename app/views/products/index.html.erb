<h1>Index of Products</h1>
<% @products.each do |product| %>
  <p><%= product[:name] %> - Price: $<%= product.formatted_price %> 
    <%= link_to "Show", "/products/#{product.id}" %> 
  </p>
<% end %>